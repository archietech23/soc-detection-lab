# Default: normalize line endings to LF in the repo
* text=auto eol=lf

# Shell scripts must always be LF (they run on Linux in CI)
*.sh text eol=lf

# Windows-specific files can stay CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf
